---
trigger: "schedule a task|schedule a.*task|add a scheduled task|set up.*scheduled task|automate.*task|create a scheduled"
---

# Schedule a Task

The user wants to schedule a task. Immediately invoke `/scheduler:add` — do not collect information upfront. The command handles all input gathering and validation.
